% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mim_threshold.R
\name{mim_threshold}
\alias{mim_threshold}
\title{Estimate a CFA-based threshold for a multi-item questionnaire}
\usage{
mim_threshold(
  mydata,
  var_formula,
  item_discretize = FALSE,
  item_levels = 10L,
  require_all_item_levels = FALSE,
  anchor_cut = 1,
  B = 0L,
  report_every = 50L,
  factor_name = "F1",
  item_suffix = "_ord",
  anchor_suffix = "_bin",
  std.lv = TRUE,
  parameterization = "theta",
  verbose = TRUE,
  ...
)
}
\arguments{
\item{mydata}{Data frame.}

\item{var_formula}{Formula of the form \code{anchor ~ item1 + item2 + ...}.
Use \code{anchor ~ .} to use all variables except the anchor as items.}

\item{item_discretize}{Logical. If \code{TRUE}, discretize each item using
\code{var_discretize()}. If \code{FALSE}, use the item values as observed ordinal
categories.}

\item{item_levels}{Number of levels to use when \code{item_discretize = TRUE}.
Must be between 2 and 12.}

\item{require_all_item_levels}{Logical. Passed to \code{var_discretize()}.}

\item{anchor_cut}{Cutpoint for binarizing the anchor if it has more than
two unique non-missing values. Values >= \code{anchor_cut} are coded 1.}

\item{B}{Number of bootstrap samples. Bootstrap CI is computed only if
\code{B >= 100}.}

\item{report_every}{During bootstrapping, print progress every
\code{report_every} attempted fits.}

\item{factor_name}{Name of the latent factor.}

\item{item_suffix}{Suffix appended to item names when items are discretized
or internally recoded.}

\item{anchor_suffix}{Suffix appended to the anchor variable name if the
anchor has more than two unique values and is dichotomized.}

\item{std.lv}{Passed to \code{lavaan::cfa()}.}

\item{parameterization}{Passed to \code{lavaan::cfa()}.}

\item{verbose}{If \code{TRUE}, print item names and generated lavaan model.}

\item{...}{Additional arguments passed to \code{lavaan::cfa()}.}
}
\value{
A \code{mim_threshold} object. Additional details can be retrieved with
\code{mim_threshold_details()}.
}
\description{
\code{mim_threshold()} estimates an anchor-based interpretation threshold for
a multi-item measure (MIM) using a one-factor CFA model with an anchor/transition
rating item.
}
\details{
This technique is based on Terluin et al (2023) and Terluin et al (2024).
Briefly, the \code{mim_threshold()} function:
\enumerate{
\item fits a one-factor CFA model with the MIM items and an anchor item,
\item computes \code{theta_star}, the latent factor value where the anchor,
threshold occurs,
\item maps \code{theta_star} to each item using CFA-implied category
probabilities,
\item multiplies item category probabilities by item values or bin
midpoints, and
\item sums the item-level expected values to obtain the MIM threshold.
}
}
\examples{
\dontrun{
sim <- simdat(N = 500, seed = 123)
dat <- sim$datw
t1_items <- sim$item_names$t1_items

dat_t1 <- dat[, c(t1_items, "trat")]

out <- mim_threshold(
  mydata = dat_t1,
  var_formula = trat ~ .,
  B = 0
)

out
}
}
\references{
Terluin, B., Koopman, J.E., Hoogendam, L. et al. Estimating meaningful thresholds
for multi-item questionnaires using item response theory. Qual Life Res 32, 1819–1830 (2023)

Terluin, B., Trigg, A., Fromy, P. et al. Estimating anchor-based minimal important
change using longitudinal confirmatory factor analysis. Qual Life Res 33, 963–973 (2024).
}
